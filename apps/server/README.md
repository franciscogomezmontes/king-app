# server (placeholder)

Real-time multiplayer server, built with Node.js + Colyseus, importing `rules-engine` directly so
game logic is never duplicated between client and server. This is Phase 6 (Online Multiplayer) in
the Notion project plan — not started yet. See "Tech Stack & Architecture" in Notion for the plan.
